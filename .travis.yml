language: java
jdk:
    - openjdk8

branches:
    only:
        - /release-v[0-9].[0-9].[0-9]/
#        - stable

install: skip
script: ./gradlew build

deploy:
    - provider: releases
      api_key: $GITHUB_TOKEN
      file_glob: true
      file: build/libs/HypixelUtils-*.*.*.jar
      skip_cleanup: true
      draft: true
      on:
          tags: true
#          branch: stable
